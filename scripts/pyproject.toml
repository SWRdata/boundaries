[project]
name = "scripts"
version = "0.1.0"
description = ""
requires-python = ">=3.12"
dependencies = [
    "requests>=2.34.2",
    "typed-argument-parser>=1.12.0",
]

[dependency-groups]
dev = [
    "ruff>=0.16.0",
    "ty>=0.0.63",
]
